{
  "name": "task-ui",
  "version": "0.0.1",
  "description": "Simple task management tool",
  "engines": {
    "node": "8.9.x"
  },
  "scripts": {
    "start": "node server",
    "heroku-postbuild": "cd react-ui/ && npm install && npm install --only=dev --no-shrinkwrap && npm run build"
  },
  "cacheDirectories": [
    "node_modules",
    "react-ui/node_modules"
  ],
  "dependencies": {
    "express": "^4.16.3"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mprestonise/task-ui.git"
  },
  "keywords": [
    "task",
    "management",
    "project",
    "react"
  ],
  "license": "MIT",
  "devDependencies": {}
}
